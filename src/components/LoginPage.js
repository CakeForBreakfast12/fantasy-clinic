import React from 'react';
import LoginForm from './LoginForm';

export const LoginPage = () => {
  return (
    <div>
      <LoginForm user={'patient'} />
    </div>
  )

};

export default LoginPage;