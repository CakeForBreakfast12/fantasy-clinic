import React from 'react';
import LoginForm from './LoginForm';

export const RegisterPage = () => {
  return (
    <div>
      <LoginForm user={'new-patient'} />
    </div>
  )

};

export default RegisterPage;